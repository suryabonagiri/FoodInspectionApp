import { ArrowRight, MapPin, Shield } from 'lucide-react'
import StatusBadge from './StatusBadge'
import HygieneScore from './HygieneScore'
import RecurringConcern from './RecurringConcern'
import SourceLink from './SourceLink'
export default function RestaurantCard({ restaurant, inspection, recurring, navigate }) {
  return <article className="panel flex h-full flex-col p-5 transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex items-start justify-between gap-3"><div><h2 className="text-lg font-extrabold text-slate-900">{restaurant.name}</h2><p className="mt-1 flex items-center gap-1.5 text-sm text-slate-600"><MapPin size={15} />{restaurant.location || 'Not available in source data'}</p></div><StatusBadge status={restaurant.currentStatus} /></div><p className="mt-3 flex items-center gap-1.5 text-xs font-semibold text-slate-500"><Shield size={14} />{restaurant.authority || 'Not available in source data'}</p><div className="my-5 flex items-center justify-between rounded-xl border border-slate-100 bg-slate-50 p-3"><HygieneScore score={restaurant.currentScore} compact /><span className="max-w-[62%] text-right text-sm leading-5 text-slate-600">{restaurant.quickInsight}</span></div><RecurringConcern concerns={recurring} />{inspection?.sourceType && <p className="mt-3 text-xs font-semibold text-slate-500">{inspection.inspectionDate ? `Inspection · ${inspection.inspectionDate}` : `News report · ${inspection.reportDate || 'Date not established'}`} · Historical findings</p>}{inspection && <div className="mt-4"><SourceLink url={inspection.sourceUrl} handle={inspection.sourceHandle} originalPostUrl={inspection.originalPostUrl} /></div>}<button onClick={() => navigate(`/restaurant/${restaurant.id}`)} className="mt-auto inline-flex items-center gap-1 pt-5 text-sm font-bold text-civic-700 hover:text-civic-900">View inspection history <ArrowRight size={16} /></button></article>
}
